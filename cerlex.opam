# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
synopsis:
  "An interleaving exploration tool for concurrent Core Erlang programs"
description: """
CerlEx explores the message interleavings of concurrent Core Erlang
   programs, retaining program states rather than replaying schedules from
   the initial state.  Its Core Erlang frontend is adapted from Caramel."""
maintainer: ["linhouy@tcd.ie"]
authors: ["Yu-Yang Lin"]
license: ["MIT" "Apache-2.0"]
depends: [
  "dune" {>= "3.11"}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
